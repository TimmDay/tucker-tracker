import { library } from '@fortawesome/fontawesome-svg-core';

import { 
  faTimes, //cross, as in exit
  faSearchLocation, //magnifying glass with a marker in it
  faSort,
  faCoffee,
  faHamburger
} from '@fortawesome/free-solid-svg-icons'

export default library.add(
  faTimes,
  faSearchLocation,
  faSort,
  faCoffee,
  faHamburger
);